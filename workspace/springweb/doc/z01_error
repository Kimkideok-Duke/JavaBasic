1. 외부 객체 생성자 멤버(구성요소) 관련 에러 발생
    ==>
    클래스를 만들고 마지막에는 ctrl+s로 반드시 저장하여야, 실행할 수 있는 컴파일
    파일이 생성된다. 외부에서 호출할 때, 클래스 구성요소가 에러가 나면 이부분을 반드시



# front단 에러처리 (js)
0. 웹서버 console에는 에러가 없고,
1. 이벤트 작동(클릭/submit등)이 되지 않을 때.
2. f12 에러라인
    - 해당 라인의 위쪽 라인과 아래쪽 라인
        if(조건){   : 중괄호, 소괄호 블럭을 빠뜨린 경우
        ==> 습관적으로 중괄호 소괄호를 시작과 닫기를 먼저하고 진행
        if(){
        }

# backend단 에러
1. console 창에서 확인 db에러 mybatis sqlSession ==> mapper
2. property 처리값 처리.. getXXX(), setXXX()
3. 404에러 대부분은
    controller단에 url mapping
    주로 요청값이 type과 메서드명(property)이 일치하지 않을 때,